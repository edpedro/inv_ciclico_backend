import { IsString, IsNotEmpty, IsNumber } from 'class-validator';

export class userInventarioAssociations {
  // @IsNumber()
  // @IsNotEmpty({ message: "O campo 'baseInventario_id' não pode estar vazio." })
  // readonly baseInventario_id: number;
  // @IsString({
  //   each: true,
  //   message: 'Cada elemento da lista deve ser uma string.',
  // })
  // @IsNotEmpty({ message: "O campo 'User_id' não pode estar vazio." })
  // readonly user_id: string;
  // readonly baseNameInventario_id: string;
  // readonly assignedBy: string;
}
